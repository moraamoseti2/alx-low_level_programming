#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main function that prints lower case alphabet letters
 * return: 0 success
*/
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		putchar(ch)
		ch++;
	}
	putchar('\n');
i	return (0);
}
